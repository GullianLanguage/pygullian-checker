import std.io

fun str.show(self: ptr[str]) : str {
    io.puts(self)
}

struct Vector[T] {
    length: int,
    items: ptr[T]
}


fun main() : int {
    let vector = Vector[str] { 0, &"hello"  }

    io.puts(vector.items.show())
}