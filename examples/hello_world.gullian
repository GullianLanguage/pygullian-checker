import std.io

struct Tag {
    text: str
    tag: ptr[str]
}

fun Tag.show(self: Tag) : str {
}

struct Book {
    title: str
    tag: Tag
}


fun main() : int {
    let book = Book { "alice", Tag { "hello", "world" } }

    io.puts(book.tag.show())
}