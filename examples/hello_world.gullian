import std.io

fun str.show(self: str) : str {

}

struct Tag[T] {
    value: T
}

struct Vector[T] {
    length: int,
    items: Tag[T]
}

fun test[T](self: Vector[T]) : T {
    io.puts(self.items)
}

fun main() : int {
    let vector = Vector[str] { 0, Tag[str] { "hello" }  }

    io.puts(vector.items.value.show())
}